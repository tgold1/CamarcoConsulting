import React from 'react';
import { Link } from 'react-router-dom';

const ProjectList = ({
    Projects,
    ShowCustomer = true,

}) => {if (!Projects.length) {
    return <h3>No Projects listed</h3>
}
return(
    
)
}